/**
 * Composition-only surface: the concrete repo app.ts (and ops scripts) wire
 * behind the PeopleRepo interface. NOT part of the module's public API.
 */
export { DrizzlePeopleRepo } from "./repo";
