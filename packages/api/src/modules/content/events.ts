/**
 * No events yet (docs/2 §3.4): media/contribution events (fingerprint
 * duplicates, placement) land with the phase-2 media work. The file exists so
 * the module keeps the uniform shape.
 */
export {};
